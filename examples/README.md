# Synthetic examples

The examples in this directory are safe to run without a Microsoft account. They contain no mailbox, tenant, site, Team, channel, document, or message data.

- `handshake.jsonl` contains an MCP initialize/notification/tools-list sequence.
- The default `m365-mcp-stdio --mock` runtime serves the same contract with in-memory fixtures.
